import { MainTemplate } from '../../templates';

export function HomePage() {
  return (
    <MainTemplate>
      <section style={{ width: '600px' }}>
        <h1>...</h1>
      </section>
    </MainTemplate>
  );
}
